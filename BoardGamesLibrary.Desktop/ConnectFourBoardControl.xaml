<UserControl x:Class="BoardGamesLibrary.Desktop.ConnectFourBoardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700">
    <Border x:Name="BoardFrame"
                Background="{StaticResource GameBoardFrameGradient}"
                BorderBrush="#1E293B"
                BorderThickness="10"
                Padding="10">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="25" ShadowDepth="8" Opacity="0.4"/>
        </Border.Effect>
        <Grid x:Name="BoardSurfaceGrid">
            <UniformGrid x:Name="BoardGrid" 
                         Rows="6" 
                         Columns="7"/>
            <Canvas x:Name="AnimationCanvas" IsHitTestVisible="False"/>
        </Grid>
    </Border>
</UserControl>
